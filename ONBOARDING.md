# korea-rio-mcp 시작 안내 (처음 사용자용)

> 이 도구는 **한국의 특정 지역에서 행사·사업에 예산을 지출하였을 때, 해당 지역
> 경제에 유발되는 생산·부가가치·고용 효과**를 산출합니다. 한국은행이 작성·공표한
> 「지역산업연관표」를 근거 자료로 사용합니다.
>
> 별도의 설치 경험이나 프로그래밍 지식은 필요하지 않습니다. 아래 절차를 순서대로
> **복사하여 붙여넣기**만 하시면 되며, 처음 이용하시더라도 약 10분이면 완료됩니다.

---

## 0. 사전 준비 (최초 1회만 설치)

이 도구는 **AI 분석 도구**(예: **Claude Code**) 위에서 동작합니다. 이용자가 자연어로
분석을 요청하면, 이 도구가 내부적으로 해당 수치를 산출합니다.

준비 사항은 다음 두 가지입니다.

1. **터미널(Terminal)** — 명령어를 입력하는 창입니다.
   - **Mac**: `command(⌘) + 스페이스` → "터미널" 입력 → 엔터
   - **Windows**: 시작 메뉴 → "PowerShell" 입력 → 엔터

2. **Claude Code** — AI 분석 도구입니다.
   설치되어 있지 않은 경우 다음을 참고하시기 바랍니다: https://claude.com/claude-code
   (설치 후 터미널에서 `claude` 입력 시 실행되면 준비가 완료된 것입니다.)

> 참고: Codex 또는 Gemini CLI에서도 동일하게 이용할 수 있습니다. 본 안내는 가장
> 접근성이 높은 **Claude Code**를 기준으로 작성하였습니다.

---

## 1. 도구 설치 (터미널에 복사하여 붙여넣기)

먼저 `uv`라는 설치 관리 도구가 필요합니다. 아래 명령을 터미널에 붙여넣고 엔터를 입력하십시오.

**Mac / Linux:**
```bash
curl -LsSf https://astral.sh/uv/install.sh | sh
```

**Windows (PowerShell):**
```powershell
powershell -ExecutionPolicy ByPass -c "irm https://astral.sh/uv/install.ps1 | iex"
```

> 설치 후 터미널 창을 **한 번 닫았다가 다시 열어** 주십시오.

이어서 본 도구를 설치합니다. 아래 명령을 붙여넣고 엔터를 입력하십시오.

```bash
uv tool install git+https://github.com/z0nam/korea-rio-mcp
```

✅ **정상 설치 시** 마지막 줄에 `Installed ... korea-rio-mcp`와 같은 문구가 표시됩니다.

---

## 2. AI 분석 도구(Claude Code)에 연결

아래 명령을 터미널에 붙여넣고 엔터를 입력하십시오.

```bash
claude mcp add korea-rio -- korea-rio-mcp
```

✅ **정상 연결 시** `Added ... korea-rio`와 같은 메시지가 표시됩니다.

연결 상태를 확인하시려면, Claude Code를 실행한 뒤(`claude` 입력) 대화창에서 다음을
입력하십시오.

```
/mcp
```

목록에 **korea-rio**가 표시되고 연결됨(connected) 상태이면 설정이 완료된 것입니다.

---

## 3. 사용 방법 (자연어로 요청)

Claude Code 대화창에 **평소 문장으로** 한국어 요청을 입력하시면 됩니다. 별도의
명령어를 숙지하실 필요는 없습니다. 예시는 다음과 같습니다.

> **"제주에서 특정 행사에 정책 예산 10억 원을 지출할 경우, 지역 경제에 유발되는
> 생산·부가가치·고용 효과를 산출해 주십시오."**

> **"현재 이용 가능한 지역 계수표 목록을 제시해 주십시오."**

> **"서울과 경기를 동일 조건으로 비교하여 표로 정리해 주십시오."**

요청을 입력하면 AI 분석 도구가 본 도구의 계산 기능을 호출하여 수치를 산출하고,
이를 표 형태로 정리하여 제시합니다. 추가로 필요한 입력 정보(예: 부문별 지출 내역)가
있는 경우, AI 분석 도구가 이를 확인하기 위해 되묻습니다.

---

## 4. 주요 기능

- 지역 행사·축제·사업의 **경제적 파급효과** 추정 (생산 / 부가가치 / 고용)
- 효과를 **지역 내 귀착분 vs 지역 외 누출분**으로 분리
- **복수 지역 비교** (동일 사업을 서울에 적용한 경우와 제주에 적용한 경우 등)
- 산출 결과를 **보고서용 표(마크다운)** 로 정리

기본적으로 **제주 2020년** 자료가 포함되어 있어 설치 직후 바로 계산이 가능하며,
그 밖의 지역 자료도 함께 제공됩니다.

---

## 5. 문제 해결

| 증상 | 조치 |
|---|---|
| `uv: command not found` | 1번 설치 후 터미널을 닫았다가 다시 열어 주십시오. |
| `claude: command not found` | Claude Code가 설치되지 않은 상태입니다. 0번 항목을 다시 확인하십시오. |
| `/mcp` 목록에 korea-rio가 없음 | 2번 연결 명령을 다시 실행한 뒤 Claude Code를 재시작하십시오. |
| 연결 실패(failed) 표시 | 터미널에서 `korea-rio-mcp`를 직접 입력해 보십시오. 오류가 발생하면 1번 설치를 다시 진행하십시오. |

---

## 추가 정보

- 프로젝트 홈: https://github.com/z0nam/korea-rio-mcp
- 상세 설명(개발자용): 위 링크의 README 참조

이용 중 막히는 부분이 있으시면, **Claude Code에 "korea-rio 사용법을 안내해 주십시오"라고
입력**하십시오. 연결이 완료된 상태라면 AI 분석 도구가 직접 안내합니다.
