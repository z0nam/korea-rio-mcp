# korea-rio-mcp 처음 사용 가이드 (초보자용)

> 이 도구는 **한국의 특정 지역에서 어떤 행사·사업에 돈을 쓰면, 그 지역 경제에
> 생산·부가가치·일자리가 얼마나 만들어지는지**를 자동으로 계산해 줍니다.
> 한국은행이 만든 「지역산업연관표」라는 공식 통계를 바탕으로 합니다.
>
> 어려운 설치나 프로그래밍 지식은 필요 없습니다. 아래 순서대로 **복사 → 붙여넣기**만
> 하면 됩니다. 처음이라도 10분이면 됩니다.

---

## 0. 준비물 (한 번만 설치하면 됩니다)

이 도구는 "AI 비서"(예: **Claude Code**) 안에서 작동합니다. AI 비서에게
계산을 시키면, 이 도구가 뒤에서 숫자를 뽑아 줍니다.

필요한 것은 딱 두 가지입니다.

1. **터미널(Terminal)** — 명령어를 입력하는 검은 창입니다.
   - **Mac**: `command(⌘) + 스페이스` → "터미널" 입력 → 엔터
   - **Windows**: 시작 메뉴 → "PowerShell" 입력 → 엔터

2. **Claude Code** — AI 비서 프로그램입니다.
   아직 없다면 설치 안내: https://claude.com/claude-code
   (설치 후 터미널에서 `claude` 라고 쳤을 때 실행되면 준비 완료입니다.)

> 💡 Codex나 Gemini CLI를 쓰셔도 됩니다. 이 가이드는 가장 쉬운 **Claude Code** 기준입니다.

---

## 1. 도구 설치 (복사해서 터미널에 붙여넣기)

먼저 `uv`라는 설치 도우미가 필요합니다. 터미널에 아래를 붙여넣고 엔터:

**Mac / Linux:**
```bash
curl -LsSf https://astral.sh/uv/install.sh | sh
```

**Windows (PowerShell):**
```powershell
powershell -ExecutionPolicy ByPass -c "irm https://astral.sh/uv/install.ps1 | iex"
```

> 설치 후 터미널 창을 **한 번 닫았다 다시 열어** 주세요.

그다음, 이 도구를 설치합니다. 터미널에 붙여넣고 엔터:

```bash
uv tool install git+https://github.com/z0nam/korea-rio-mcp
```

✅ **성공하면** 마지막 줄에 `Installed ... korea-rio-mcp` 비슷한 문구가 나옵니다.

---

## 2. AI 비서(Claude Code)에 연결하기

터미널에 아래 한 줄을 붙여넣고 엔터:

```bash
claude mcp add korea-rio -- korea-rio-mcp
```

✅ **성공하면** `Added ... korea-rio` 같은 메시지가 보입니다.

연결이 잘 됐는지 확인하려면, Claude Code를 실행한 뒤(`claude` 입력) 대화창에서:

```
/mcp
```

이라고 입력하세요. 목록에 **korea-rio** 가 보이고 초록불(connected)이면 끝입니다.

---

## 3. 처음 써보기 (그냥 한국어로 물어보세요)

Claude Code 대화창에 **평소 말하듯이** 한국어로 부탁하면 됩니다.
명령어를 외울 필요 없습니다. 예시:

> **"제주에서 어떤 행사에 정책비 10억 원을 쓰면 지역 경제에 생산·부가가치·고용이
> 얼마나 유발되는지 계산해줘."**

> **"지금 쓸 수 있는 지역 계수표 목록을 보여줘."**

> **"서울과 경기를 같은 조건으로 비교해서 표로 정리해줘."**

AI가 알아서 이 도구의 계산 기능을 불러 숫자를 뽑고, 표로 정리해 줍니다.
무엇을 더 알려줘야 하는지(예: 어떤 부문에 얼마를 쓰는지)도 AI가 되물어 줍니다.

---

## 4. 이런 걸 할 수 있어요

- **지역 행사·축제·사업의 경제적 파급효과** 추정 (생산 / 부가가치 / 고용)
- 효과를 **지역 안에 남는 몫 vs 지역 밖으로 새는 몫**으로 분리
- **여러 지역 비교** (같은 사업을 서울에 vs 제주에 했을 때)
- 결과를 **보고서용 표(마크다운)** 로 정리

기본으로 **제주 2020년** 자료가 들어 있어 바로 계산됩니다. 다른 지역도 포함되어 있습니다.

---

## 5. 잘 안 될 때

| 증상 | 해결 |
|---|---|
| `uv: command not found` | 1번 설치 후 터미널을 닫았다 다시 여세요. |
| `claude: command not found` | Claude Code가 아직 안 깔린 것입니다. 0번을 다시 보세요. |
| `/mcp` 목록에 korea-rio가 없음 | 2번 연결 명령을 다시 실행하고 Claude Code를 재시작하세요. |
| 빨간불 / failed | 터미널에서 `korea-rio-mcp` 라고 직접 쳐보세요. 오류가 나면 1번 설치를 다시 하세요. |

---

## 더 알아보기

- 프로젝트 홈: https://github.com/z0nam/korea-rio-mcp
- 자세한 설명(개발자용): 위 링크의 README

질문이 막히면, **Claude Code에게 "korea-rio 사용법 알려줘"라고 물어보세요.**
연결돼 있으면 AI가 직접 안내해 줍니다.
